# 🏅 SportConnect App

SportConnect, sporla ilgilenen bireyleri ve takımları bir araya getirmeyi amaçlayan bir mobil uygulamadır.  
Kullanıcılar spor etkinlikleri oluşturabilir, etkinliklere katılabilir ve ortak ilgi alanlarına sahip kişilerle bağlantı kurabilir.

Bu proje **Expo + React Native** kullanılarak geliştirilmiştir.

---

## 🚀 Özellikler

- Kullanıcı profili oluşturma  
- Spor etkinliği oluşturma ve etkinliklere katılma  
- Etkinlik listelerini görüntüleme  
- Mobil uyumlu modern arayüz  

> Not: Bazı özellikler geliştirme aşamasındadır.

---

## 🛠 Kullanılan Teknolojiler

- React Native  
# Fitness & Sports Social App

A comprehensive mobile application connecting fitness enthusiasts, enabling them to discover events, join activities, and track their sports journey.

## 📱 Features

- **Discover Events**: Browse and filter sports events by category (Running, Cycling, Yoga, etc.).
- **Join Activities**: Seamlessly join events and manage your schedule.
- **Social Connection**: View user profiles and connect with other athletes.
- **Interactive UI**: Modern, dark-themed design with smooth animations.

## 🚀 Getting Started

### Prerequisites

- Node.js installed
- Expo Go app on your mobile device (Android/iOS)

### Installation

1. Clone the repository:
   ```bash
   git clone <repository-url>
   ```
2. Navigate to the project directory:
   ```bash
   cd fitness-social-app
   ```
3. Install dependencies:
   ```bash
   npm install
   ```
4. Start the application:
   ```bash
   npx expo start --clear
   ```

## � APK Generation (For Android)

To generate an APK file for manual installation on Android devices:

1. **Install EAS CLI**:
   ```bash
   npm install -g eas-cli
   ```

2. **Login to Expo**:
   ```bash
   eas login
   ```

3. **Configure Build**:
   ```bash
   eas build:configure
   ```

4. **Build APK**:
   ```bash
   eas build -p android --profile preview
   ```
   *Note: This usually requires a free Expo account.*

## 🛠 Tech Stack

- **Framework**: React Native (Expo)
- **UI Architecture**: Component-based layout
- **Navigation**: React Navigation (Stack & Bottom Tabs)
- **Styling**: Custom Theme System

---
**Course Project**
/
│
├── app/ # Sayfalar (Expo Router)
├── assets/ # Görseller ve ikonlar
├── components/ # Tekrar kullanılabilir bileşenler
├── constants/ # Sabitler
└── README.md  
---

## ⚙️ Kurulum ve Çalıştırma

Projeyi çalıştırmak için aşağıdaki adımları izleyin: npm install
npx expo start  
Başlatma sonrası uygulama:
- Expo Go
- Android Emulator
- iOS Simulator

üzerinden çalıştırılabilir.

---

## 🧑‍💻 Geliştirme

Uygulama geliştirme işlemleri `app/` klasörü içerisindeki dosyalar üzerinden yapılmaktadır.  
Bu projede **dosya tabanlı yönlendirme (file-based routing)** kullanılmaktadır.

---




